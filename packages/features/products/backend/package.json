{
  "name": "@workspace/products-backend",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup src --dts",
    "dev": "tsup src --watch",
    "check-types": "tsc --noEmit",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@workspace/backend-utils": "workspace:*",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@workspace/typescript-config": "workspace:*",
    "@types/node": "^24.0.0",
    "tsup": "^8.3.5",
    "typescript": "5.9.2"
  }
}
